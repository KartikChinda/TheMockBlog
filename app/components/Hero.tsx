import React from "react";

const Hero = () => {
  return (
    <div className="h-[80vh] flex flex-col lg:flex-row justify-between items-center">
      <div className="w-full lg:w-[70%]">
        <div>The Mock Blog</div>
        <div>It's always an empty page, until it is not.</div>
      </div>
    </div>
  );
};

export default Hero;
